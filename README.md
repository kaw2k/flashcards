# Getting Started

```bash
# Setup the repo
git clone https://github.com/kaw2k/flashcards.git
cd flashcards

# Install dependencies, you will need Node v12.3.1
nvm use
npm install

# Set up firebase
cp .template.env .env
# Create a firebase web instance and copy the config values
# into the .env file you created
```
