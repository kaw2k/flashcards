import React from 'react'
import { Session } from 'src/helpers/generateQuiz'

export const Quiz: React.FC<{ session: Session }> = ({ session }) => {
  const [prompt, setPrompt] = React.useState()

  return <div>sup</div>
}
